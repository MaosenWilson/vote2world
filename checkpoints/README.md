# Checkpoints Directory

Store server-side model assets here:

```text
checkpoints/
  tokenizer/       RT-1 single-step tokenizer.
  base/            Base visual world model checkpoint.
  rlvr/            GT-based RLVR checkpoint for comparison.
```

Model weights are ignored by git.

